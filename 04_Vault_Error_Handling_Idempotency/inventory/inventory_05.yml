all:
  hosts:
    localhost_01:
      ansible_connection: local
    localhost_02:
      ansible_connection: local
  vars:
    ansible_user: ubuntu
    ansible_port: 22
    ansible_ssh_private_key_file: /home/ubuntu/.ssh/ansible_managed_node.pem

  children:
    webservers:
      hosts:
        web01:
          ansible_host: 

    databases:
      hosts:
        db01:
          ansible_host: 
